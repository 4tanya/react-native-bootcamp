export interface BadgeRowProps {
  title: string;
  value: string | undefined;
}

export default interface BadgeProps {
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  memberId: string;
}
