import {StyleSheet} from 'react-native';
import {colors} from '../../styles/_base';

export default StyleSheet.create({
  label: {
    color: colors.drawerColor,
    fontWeight: 'bold',
  },
  button: {
    color: colors.drawerButtonColor,
    fontWeight: 'bold',
  },
});
